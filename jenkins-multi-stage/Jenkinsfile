pipeline{
      agent {lable 'MyNode'}
stages {
        stage ('Clone') {
            steps {
                git branch: 'main', url: "https://github.com/hossain109/CICDMavenProject.git"
            }
        }
}
